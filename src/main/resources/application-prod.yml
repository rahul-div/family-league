spring:
  jpa:
    show-sql: false
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10

logging:
  level:
    com.familyleague: INFO
    org.hibernate.SQL: WARN
    root: WARN
